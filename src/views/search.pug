extends base.pug
include mixins/video.pug

block content 
  form(method="GET").search__form
    input(name="keyword",placeholder="Search by text",type="text")
    button
      i.fas.fa-search
    div.video-grid
      each video in videos 
        +video(video)
      else
        span.empty__message No videos found